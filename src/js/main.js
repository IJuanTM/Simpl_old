/**
 * Define urls to use in js
 */

// Baseurl for accessing files from the root folder of the project
const baseUrl = window.location.origin + '/';

// An array of all the url parts
const urlArr = window.location.pathname.split('/');